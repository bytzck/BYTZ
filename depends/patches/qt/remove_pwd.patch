--- a/qtbase/configure	2022-01-08 14:31:12.474408914 +0000
+++ b/qtbase/configure	2022-01-08 14:31:42.082958056 +0000
@@ -36,9 +36,9 @@
 relconf=`basename $0`
 # the directory of this script is the "source tree"
 relpath=`dirname $0`
-relpath=`(cd "$relpath"; /bin/pwd)`
+relpath=`(cd "$relpath"; pwd)`
 # the current directory is the "build tree" or "object tree"
-outpath=`/bin/pwd`
+outpath=`pwd`
 
 WHICH="which"
 
